# Copy to .env for docker compose: cp .env.example .env

# Required for fix/PR commands — create at https://github.com/settings/tokens
# Scopes: repo, workflow
GH_TOKEN=

# Optional: mount host gh keyring instead of token
# GH_CONFIG_DIR=/home/you/.config/gh