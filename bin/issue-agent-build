#!/usr/bin/env zsh
# Full build: polish + collect + fix all repos (incl. tinygrad + vision local queue)
source "$HOME/.config/cockpit/secrets.env" 2>/dev/null
export PATH="$HOME/.local/bin:$HOME/bin:$PATH"
export OLLAMA_HOST="${OLLAMA_HOST:-http://127.0.0.1:11434}"
export OLLAMA_API_BASE="${OLLAMA_API_BASE:-$OLLAMA_HOST}"
exec "$HOME/.local/venvs/aider/bin/python" "$HOME/issue-agent/issue_agent.py" build "$@"